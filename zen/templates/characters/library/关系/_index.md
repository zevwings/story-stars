# [本角色]｜关系档索引

> 本页是「本角色」**唯一**的关系摘要 / 索引层, 每行一句; [主卡](../主卡.md) 不再维护关系表。只登记作者已经确认的静态关系定位, 候选关系留在 `.analysis/`。需要完整静态配对档时, 只在 owner 一侧建立 `<对象>.md`(见 [_template](_template.md), 可选); 非 owner 一侧用相对 Markdown 链接指向实际档案。
>
> 本表按当前角色视角摘要。同一关系可以在双方索引中各有一行, 分别记录各自的关系意义、责任与边界, 不要求文字一致, 也不机械补齐另一侧。owner-centric 只约束完整 `关系/<对象>.md` 配对档: 全项目只保留一份; 双方摘要若都存在, 可共同指向该档。`sw rag characters address` 只负责配对档中的有向称呼。
>
> `对象` 使用正式姓名。需要在括号中展示艺名或稳定常用名时, 该名称必须已经登记在对象的 `主卡.md`; 括号仅用于显示, 不构成新的姓名 / 别名真源。双方的有向称呼只写入 `关系/<对象>.md`, 关系的按章推进只写入 `context/characters/[本角色].md`。
>
> 下表是最小公共契约。项目确有稳定用途时可增加 `分型`、`慎用方向` 等静态列, 但不要保留空列, 也不要增加称呼或动态阶段列。

| 对象 | 关系性质 | 结构位置 / 关系重量 | 一句定调 / 写作边界 | 配对档 |
| --- | --- | --- | --- | --- |
| | | | | [有则填相对 Markdown 链接; 当前角色是 owner 时通常指向 `<对象>.md`] |
