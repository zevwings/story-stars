# 视觉元数据

本目录保存角色视觉工作的文本真源，包括请求、Prompt、审批记录、清单和当前版本指针。

- 不存放全尺寸生成图片；图片保存在项目根目录下的 `visual-assets/` 独立 Git 仓库。
- 只有作者明确接受的结果可以进入稳定记录；未接受候选留在图片仓库的临时工作区。
- 视觉记录辅助写作与后续生图，不自动成为人物设定或正文正典。
- Skill 不执行 Git commit、push 或远端配置。
