# [本角色]｜关系档索引

> 本页是「本角色」**唯一**的关系摘要 / 索引层，每行一句；[主卡](../主卡.md) 不再维护关系表。需要完整静态配对档时在本目录建 `<对象>.md`（见 [_template](_template.md)，可选）。
>
> 角色有任何值得登记的关系即建本页；无关系可记时可不建 `关系/` 目录。关系的按章推进走 `context/characters/[本角色].md`，不写在这里。
>
> 本表按当前角色视角摘要。同一关系可以在双方索引中各有一行，分别记录各自的关系意义、责任与边界，不要求文字一致，也不因一侧已登记就机械补齐另一侧。owner-centric 只约束完整 `关系/<对象>.md` 配对档：全项目只保留一份；双方摘要若都存在，可共同指向该档。
>
> 本索引不登记剧情节点、剧情阶段或未来事件；剧情节点从剧情侧引用人物卡或静态关系档。
>
> `对象` 为已登记人物时使用其主卡正式姓名；确需记录组织、匿名责任方或程序接口时，使用已有正式名称或稳定描述，不为补齐关系表虚构人物卡。

| 对象 | 关系性质 | 结构位置 / 关系重量 | 一句定调 / 写作边界 | 配对档 |
| --- | --- | --- | --- | --- |
|  |  |  |  | [有则填 `<对象>.md`] |
