# PL-X-000｜节点名称

> 本页是正式剧情节点模板。模板存在不代表节点已经进入正典；复制到 `主线/`、`副线/` 或 `亲密线/` 并由作者确认后，才作为正式剧情节点登记。
>
> 只保存已经确认且后续章节必须继承的变化。已经确认的阶段顺序、场景功能与执行边界统一写入 `story/construction/`，并由本页“施工单元接口”链接。
>
> 具体对白、完整动作、身体流程、衣装细节、精确章号、字数、方案比较、候选方案与未决参数不得写入本页。

## 节点信息

- **节点 ID**：`PL-X-000`（主线用 `M`、副线用 `S`、亲密线用 `I`；序号按类型递增）
- **线路类型**：主线 / 副线 / 亲密线
- **阶段归属**：阶段一 / 阶段二 / 阶段三 / 阶段四 / 阶段五 / 尾声 / 既往前史
- **节点形态**：推进节点 / 回路节点
- **推进结构**：单点 / 递进链（仅推进节点填写；回路节点删除本行）
- **节点定位**：
- **归属目录**：
- **主体类型**：人物线 / 关系线 / 事件线 / 势力线 / 群像线
- **归档类型**：人物 / 组织（多人主体仍归人物；主线、副线据此进入对应二级目录；亲密线仍直接按第一剧情主体归档）
- **归档主体**：
- **中心人物**：（一人或多人；无固定人物时写“无固定中心人物”。本字段不等于章节 POV）
- **当前状态**：pending

> `阶段归属`只记录本节点状态变化正式完成的主阶段。回路节点记录首次稳定建立阶段；后续重复调用不追加阶段。已经发生、只在正文中逐步揭露的因果写“既往前史”。如果一个节点包含两个阶段各自独立的可继承变化，应拆成两个节点，不写阶段范围。

## 前置状态

- **人物前置**：
- **关系前置**：
- **世界观 / 势力前置**：
- **线索前置**：

## 必须发生

-

## 本线变化

- **人物变化**：
- **关系变化**：
- **知情范围变化**：
- **世界观 / 势力变化**：
- **亲密兑现变化**：（无亲密线职责时删除）

## 完成判据

> 完成判据必须能被正文或定稿摘要验证；不得固定具体对白、动作、场次数量或实现篇幅。

- [ ]

## 后续接口

- **上游节点**：
- **下游节点**：
- **相关主线 / 副线 / 亲密线**：
- **章节大纲接口**：
- **亲密专项接口**：（仅亲密线或需要 intimacy 承接时保留）
- **施工单元接口**：（存在已确认执行规格时，链接到 `story/construction/` 中的 `CON-xxxx`；无则删除）

## 不得提前 / 不得写偏

-

## 正式真源引用

> 这里只列使本节点成立的事实依赖。允许引用 `BIBLE.md`、`story/genres/`、`characters/`、`worldbuilding/`、`clues/`、已启用专项及其他正式剧情节点。
> 阶段归属不得通过引用 `story/arcs/` 表达；施工单元只写入上方接口；不得引用 `chapters/`、`context/`、`.analysis/`、`.prewrite/` 或 `.rag/`。

-
