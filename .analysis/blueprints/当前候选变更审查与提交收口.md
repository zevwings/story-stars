# 当前候选变更审查与提交收口

> 日期：2026-08-09
> 状态：待完成
> 性质：分析层收口清单，不授权提交、删除、回退或覆盖现有修改

## 一、目标

把当前关系分层、人物关系、剧情节点、施工、阶段弧线与时间线的大范围未提交候选变更，收敛成可审查、可验证、可精确暂存的改动集。

## 二、当前证据

- 基线：分支 `zevwings/0808_yunding_trio_private_relation`，`HEAD e360440d`。
- 当前 tracked diff 基线：133 个文件，900 行新增、1597 行删除；另有未跟踪的分析、原型、剧情节点、施工和 `visuals/` 路径。
- 已通过：`git diff --check`、`sw plotline validate`、`sw rag construction validate`、`sw check references`、`sw check links`。
- 尚未完成：完整故事变更审查、未跟踪文件归属确认、提交范围划分、精确暂存、提交及提交后复验。

以上数字只记录本蓝图建立时的工作树快照；恢复时必须重新读取 `git status` 与 diff，不能把它当作当前事实。

## 三、必须完成

1. 以当前 unstaged 与相关 untracked 文件为范围运行 `story-review`，重点核对删除／重命名后的语义承接、关系终点、阶段容量、状态索引和施工接口。
2. 区分本批关系改动、顾清言视觉原型候选、`visuals/` 资产与其他归属不明改动；不得把未确认原型或派生资产混入正式故事提交。
3. 对 review finding 做最小修复，再重新运行五项验证。
4. 获得作者提交授权后，按独立逻辑批次精确暂存；未经授权不提交。
5. 验证暂存范围、提交 OID 与工作树剩余改动，确认没有覆盖或夹带用户已有修改。

## 四、完成判据

- 目标改动不存在阻塞性 story-review finding；
- 五项结构与链接检查通过；
- 所有 untracked 文件都有明确归属：纳入、保留未跟踪或移出本批；
- 提交范围只包含已确认路径；
- 分支、提交和剩余 dirty path 状态已记录。

## 五、红线

- 不使用整文件 checkout、reset 或批量清理恢复工作树。
- 不把 `.analysis`、`.prototypes` 或 `visuals/` 自动视为正式故事事实。
- 不因校验器通过就跳过内容层审查。
- 不在本批顺手开始开篇施工或白玥五项剧情决策。
